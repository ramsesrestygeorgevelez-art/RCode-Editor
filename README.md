# RCode-Editor

## A code editor that is the same as VSCode and Acode but combined.

# Code to make an plugin:

``` typescript

const myPlugin = new RCodePlugin("MyPlugin")

myPlugin.setWiki("example.com")
myPlugin.createSetting("nameofsetting", "value")
```